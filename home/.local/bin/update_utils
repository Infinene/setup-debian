#!/usr/bin/bash
# set -x

if [ "$(whoami)" != "root" ]; then
  SUDO=sudo
fi

this_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
arch=$(dpkg --print-architecture)

# If sudo, get the password now
${SUDO} echo

. $this_dir/bin.d/get_latest.sh

if [ $# -ne 0 ]; then
	. $this_dir/bin.d/update_"$1".sh
else
  . $this_dir/bin.d/update_micro.sh
  . $this_dir/bin.d/update_eza.sh
  . $this_dir/bin.d/update_lf.sh
  . $this_dir/bin.d/update_fd.sh
  . $this_dir/bin.d/update_bat.sh
  . $this_dir/bin.d/update_ripgrep.sh
  . $this_dir/bin.d/update_composer.sh
fi
