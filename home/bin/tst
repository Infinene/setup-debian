#!/bin/bash
set -x

# source log.sh
# create_log 'tst.log'
# log

if command -v rg &> /dev/null
then
  echo "command exists"
else
  echo "command not found"
fi

if command -v rg &> /dev/null
then
  CUR_VERSION="$(rg -V)"
else
  CUR_VERSION="0.0"
fi